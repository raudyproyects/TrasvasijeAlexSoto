<h2>Editar <%= resource_name.to_s.humanize %></h2>

<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f| %>
  <%= devise_error_messages! %>

  <div><%= f.label :email, "Correo" %><br />
  <%= f.email_field :email, :autofocus => true %></div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div><%= f.label :password, "Nueva Contraseña" %> <i>(Dejar en blanco si no desea cambiarla)</i><br />
  <%= f.password_field :password, :autocomplete => "off", placeholder: "********" %></div>

  <div><%= f.label :password_confirmation, "Confirmar nueva contraseña" %><br />
  <%= f.password_field :password_confirmation, placeholder: "********" %></div>

  <div><%= f.label :current_password, "Contraseña actual" %> <i>(Para confirmar los cambios)</i><br />
  <%= f.password_field :current_password, placeholder: "********" %></div>

  <div><%= f.submit "Actualizar" %></div>
<% end %>

<h3>Desactivar mi cuenta</h3>

<p>Desactivar tu cuenta significa que no podrás volver a ingresar como empleado. <%= button_to "Eliminar", registration_path(resource_name), :data => { :confirm => "Está seguro?" }, :method => :delete %></p>

<%= link_to "Volver", :back %>
